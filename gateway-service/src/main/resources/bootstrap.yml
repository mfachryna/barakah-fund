spring:
    application:
        name: gateway-servic
    cloud:
        config:
            enabled: false
        vault:
            enabled: false

# spring:
#     application:
#         name: gateway-service
#     # config:
#         # import: "optional:configserver:http://localhost:8888"
#         # import: "vault://"
#     cloud:
#         config:
#             enabled: false
#             fail-fast: false
#         vault:
#             # kv:
#             #     backend: kv
#             enabled: ${SPRING_CLOUD_VAULT_ENABLED:false}
#             uri: ${SPRING_CLOUD_VAULT_HOST:localhost}
#             host: ${SPRING_CLOUD_VAULT_URI:http://127.0.0.1:8200}
#             port: ${SPRING_CLOUD_VAULT_PORT:8200}
#             scheme: ${SPRING_CLOUD_VAULT_SCHEME:http}
#             authentication: TOKEN
#             token: ${SPRING_CLOUD_VAULT_TOKEN:root-token}

# # # Default local development settings
# # server:
# # port: 8081

# # grpc:
# #     server:
# #         port: 9091

# # management:
# #     endpoints:
# #         web:
# #             exposure:
# #                 include: health,info

# # logging:
# #     level:
# #         org.springframework.cloud.config: DEBUG
# #         org.springframework.web.client: DEBUG
